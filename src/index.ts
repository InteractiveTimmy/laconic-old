// Copyright 2019

/**
 * Main entry file for the Laconic SDK. Laconic is a small and elegant web
 * component framework that utilizes the Web Component API to help streamline
 * the generation of reactive web applications.
 *
 * @remarks
 * Utilizing Lacanoc requires extending the exposed `Element` class and
 * overriding the provided methods documented in the class definition
 * of the `Element` class documentation. The general rendering model
 * closely reflects that of which is seen in Facebook's
 * {@link https://github.com/facebook/react | React} repository.
 *
 * @packageDocumentation
 */

import {
  Element,
} from './models/index';

export default {
  Element,
};
